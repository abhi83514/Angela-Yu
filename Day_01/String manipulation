# String concatenation is simply merging seperate strings into one. 

print("Abhi"+" "+"Rajput")

# If we added a space or use a tab key before the code then we face a IndentationError. 

    print("Get a error")
